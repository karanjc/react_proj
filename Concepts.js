// foreach:

const users = [{firstName: "Karan", age: 23},
{firstName: "abc", age: 22},
{firstName: "def", age: 21},
]

users.forEach((user) => {
    console.log(user.firstName);
});


// usestate
const evenNumbers = numbers.filter(filterisOdd);
console.log(evenNumbers);